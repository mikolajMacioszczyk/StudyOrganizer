<Window x:Class="StudyOrganizer.WPF.Views.NewSubjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:StudyOrganizer.WPF.Views"
        mc:Ignorable="d" Icon="../Images/studyIcon.png"
        Title="NewSubjectView" Height="450" Width="600">
    <Window.Background>
        <ImageBrush ImageSource="../Images/study.jpg" />
    </Window.Background>
    <StackPanel>
        <TextBlock Text="Nazwa Przedmiotu:" />
        <TextBox x:Name="SubjectName" />
        
        <TextBlock Text="Typ Zajęć:" />
        <ComboBox x:Name="TypeComboBox"></ComboBox>
        
        <TextBlock Text="Dzień tygodnia:" />
        <ComboBox x:Name="DayOfWeekComboBox">
            <DataTemplate>
                <TextBlock Text="{Binding}" />
            </DataTemplate>
        </ComboBox>
        
        <StackPanel>
            <TextBlock Text="Godzina:" />
            <Label />
        </StackPanel>
        <Slider />
        
        <StackPanel Orientation="Horizontal">
            <Button Content="Accept" Click="Accept_OnClick" />
            <Button Content="Dismiss" Click="Dismiss_OnClick" />
        </StackPanel>
    </StackPanel>
</Window>
