<UserControl x:Class="StudyOrganizer.WPF.UserControls.SubjectTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StudyOrganizer.WPF.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Width="200" Height="100" Margin="10"
             Background="#cce6fc" BorderBrush="#a6d1f7"
             BorderThickness="1,1,4,4" CornerRadius="5">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Path=Name}" TextAlignment="Center" Margin="3" FontSize="18" />
            <TextBlock Text="{Binding Path=Type}" TextAlignment="Center" Margin="2" FontSize="16" />
            <TextBlock Text="{Binding Path=WeeklyDate}" TextAlignment="Center" Margin="3" FontSize="14" />
        </StackPanel>
    </Border>
</UserControl>
